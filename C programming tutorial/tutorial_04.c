#include <stdio.h>
int main(){
    // variable declaration
    int a;
    // variable intialization
    float b=3.5;
    // constant creation after this the const we create is immovable 
    const int c=7;
    printf("Enter the value a variable \n");
    scanf("%d",&a);
    printf("the value of a,b and c %d,%f,%d",a,b,c);
    return 0;
}