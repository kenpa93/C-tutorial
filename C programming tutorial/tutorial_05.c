#include <stdio.h>
int main(){
    // its an array in c language
    int a[5]={1,2,3,4,5};
    printf("the value at position 3 %d \n",a[3]);
    for ( int i = 0; i < 5; i++)
    {
        /* code */
        printf(" %d \n",a[i]);
    }
    
    return 0;
}